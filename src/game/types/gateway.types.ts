export enum GameActions {
	frame = 'game/frame',
	input = 'game/input',
}

export enum LobbyActions {
	create = 'lobby/create',
	find = 'lobby/find',
	join = 'lobby/join',
	delete = 'lobby/delete',
}
